# Enum: UserRole

**Path:** `Backend/VTA.Data/Models/UserRole.cs`

## Overview
The `UserRole` enum defines the different roles a user can have within the system.

## Members
- `Child` (0): Represents a child user.
- `Caregiver` (1): Represents a caregiver user.
- `Admin` (2): Represents an administrative user with elevated privileges.

## Internal Imports
- `VTA.Data.Models` (namespace)

## Notable Packages
*(None beyond standard C# libraries)*